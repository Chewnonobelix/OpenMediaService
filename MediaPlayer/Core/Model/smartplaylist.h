#pragma once

#include "playlist.h"

class SmartPlaylist : public PlayList {
	Q_OBJECT

private:
public:
	SmartPlaylist() = default;
	SmartPlaylist(const SmartPlaylist &) = default;
	~SmartPlaylist() = default;

public slots:
};
